# Core dependencies (required)
streamlit>=1.30
pytest>=7.0

# Phase 1 SafeCare AI — all stdlib; no extra installs needed.
# Optional LLM support (not used in Phase 1):
#   pip install anthropic   # then set ANTHROPIC_API_KEY env var
